import { Typography } from '@mui/material';
import React from 'react';

const Logo = () => {
  return (
    <Typography
      sx={{
        fontFamily: '"Pacifico", cursive',
        fontWeight: 300,
        fontSize: 30,
        fontStyle: 'normal',
      }}
    >
      Simply Schedule
    </Typography>
  );
};

export default Logo;
